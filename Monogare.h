#ifndef MONOGARE
#define MONOGARE
	#include "monotypes.h"
	booleen achatgare(Joueur*, station*);
	void acquisgare(Joueur*, station*);
	void pertegare(Joueur*, station*);
	void paiementloyergare(Joueur*, int, station*);
	booleen casegare(Joueur*, station*);
	void venteEncheresGare(station*, listeJoueur*);
	station* returngare(Joueur*, station*);
	int nbgarepossede(Joueur*, station*);
	void visuelgare(Joueur*, station*);
#endif
